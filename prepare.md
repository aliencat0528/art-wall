# Art Wall — 決策記錄

> **版本**：MR-017 · 2026-07-31
> **記錄規則繼承根目錄 `prepare.md`**，此處只記差異。

---

## 本專案差異

- **編號前綴**：`MR-<三位數>`
- **容量**：只留最近 10 筆，超出移到 `prepare-archive.md`（MR-014 移出 MR-001～MR-004，MR-015 移出 MR-005，MR-016 移出 MR-006，MR-017 移出 MR-007）
- **上浮**：決策若影響其他子專案 → 請主線開 `D-00X`，本檔案該筆改標 `→ D-00X`
- **實作順序**
  - **Phase 1**（PR #2，一次 merge）：MR-001 + MR-002 + MR-003 + MR-005 + MR-006
    - MR-006 是 Phase 1 首版實作後、瀏覽器實測才追加的視覺重整，決策日期較晚但同屬 Phase 1。
      不另開 Phase：重整會重畫 MazeMap，而 Phase 1 已知的兩個 bug（房間標籤錯位、
      點選無反應）都住在該元件，先修再重畫是白工。
  - **Phase 2**：MR-004 + 密室
  - **Phase 3**：房間互動升級 + 真實資料 + deploy
  - **未排程**：待討論事項 #1（個人化內容編輯）
  - **→ D-003 轉向**：改採作品牆 Phase A/B/C（見 MR-007）；舊 Phase 2/3 之密室與房間互動併入 Phase B/C 待重估
  - **→ 2026-07-22 重排**：Phase A 已完成（MR-008／MR-009）。其後改為
    - **Phase D**：部署上線（MR-010，已完成）
    - **Phase E**：資料模型一次改完＝分類可編輯＋策展層（MR-012，已完成，PR #10–#12）
    - **Phase F**：視覺互動＝長廊景深＋詳情頁 pan/zoom（純視圖層，不動資料）**已完成**
      - 長廊那半（MR-014，PR #17）：暗場光氛定調 + 長廊景深、拖曳、逐件停留
      - 詳情頁 pan/zoom（MR-015）：雙擊／滾輪／拖移、手機雙指，上限原圖 1:1
    - **Phase G**：展場漫步彩蛋（原 Phase B，須待 E／F 穩定）**已定案，實作中（MR-017）**
    - 合併 E 的理由：兩者都動 `types`／`useLibrary`／`EditorPanel`，分兩次做等於改兩遍

---

## 待討論事項

| # | 議題 | 卡在什麼 | 提出日期 |
|---|------|---------|---------|
| 1 | 是否要做「零手動」發佈 | MR-010 已給出手動路徑（匯出 JSON → 寫進 `data/works.ts` → PR → 自動部署），可用但每次都要進開發流程。要做到「上傳完就對外可見」需後端或 GitHub API 寫回＋token，等真作品資料到位再評估 | 2026-07-21（MR-010 收斂） |
| 4 | 標題要不要改用襯線字 | 原型 C／F 用系統襯線當標題，明顯拉高展場感，但主站尚未採用：系統襯線在不同平台落差大（`Songti TC` / `Noto Serif TC` 未必有），自帶 web font 則要付首屏成本。未決的是「首屏成本能吃多少」 | 2026-07-25（原型觀察，MR-014 未處理） |

> **已解**：原 #2「展場感視覺方向」與 #3「書法／立體 accent 無彩」由 MR-014 定案；
> 原 #2「Phase F 的另一半：詳情頁 pan/zoom」由 MR-015 定案並落地（三個決定：標準手勢集／沿用 `view` 1800px／手機只給雙指）；
> 原 #5「疊印色場的四項視覺」由 MR-016 定案並落地——與中性原則的衝突以「四項全部落在作品以外」收斂；
> 原 #3「Phase G 第一人稱漫步要不要做」由 MR-017 定案為**做**；其手機那一半（原列為 #5）
> 亦於同日由用戶拍板**接受降級**，不退回網格，見 MR-017「手機」段。

---

## 決策日誌（最近 10 筆，新的在上）

### MR-017 · 2026-07-31 · wall
- **決策**：Phase G 第一人稱走廊**要做**，並定四件事：
  1. **暗場，不做明場**——走廊沿用 MR-014 的唯一定調。用戶提供的白盒走廊參考圖不照搬其光
  2. **幾何比照參考圖**：左右牆 `rotateY(±90deg)`、作品沿 Z 軸等距後退、消失點置中、
     離散步進（一次一件，配 `BACK`／`WALK ON`），不做自由漫遊
  3. **地板＝光波流動**：常駐慢流是氛圍，走動時加速才是「我在移動」的回饋
  4. **第三種瀏覽模式**（依媒材／依展覽／走進展場），**不可為預設入口**
- **用戶指定的三條硬要求**：切換與移動時**不卡住、不跑版、作品仍點得開**。
  這三條不是驗收項而是設計約束，直接決定下面的結構選擇
- **理由（明場那條是這筆的核心）**：參考圖的說服力有相當比例來自白牆與灰階陰影，
  那是白盒美術館的語言。搬進暗場只有兩條路——走廊也暗場（拿不到那張圖的樣子），
  或走廊自己明場。後者等於站台同時存在兩套光，而 MR-014 收成單一定調的理由正是
  「只有一套呈現時，每個光學樣式都不必寫成條件分支」。**用戶選暗場，這條收斂掉了。**
- **選項比較**（三個方向並置預覽，用戶據此拍板）：
  | 方向 | 空間感來源 | 踩到幾條限制 |
  |------|-----------|------------|
  | A 維持現況 | 光與景深暗示（已上線） | 0 / 8 |
  | B 側牆＋透視地板 | 牆與地板界定 | 0 / 8 |
  | C 第一人稱 | 透視 ＋ 視點位移 | **6 / 8** |
- **棄選**：
  - **A 維持現況**：拿不到「走進去」，而用戶要的正是那個
  - **B 側牆**：成本最低且流動地板在 B 就成立（流動要的只是收向消失點的地板，
    不是第一人稱），但它給不了視點位移。**B 未被否定，只是不足**——
    C 的側牆與地板本就是 B 的超集，日後若回頭做 B 不會浪費
  - **明場走廊**：見上，會讓站台有兩套光
  - **自由漫遊**：等於自製遊戲引擎；參考圖本身就是離散步進
- **第八條限制（MR-016 之後才出現，知識檔原本沒有）**：顆粒（`body::after`，`z-index: -1`）
  與疊印色場都是 `fixed` 背景層，**不參與 3D 透視**。往前走時它們紋風不動，
  大腦拿固定紋理當參考系會把「場景在動」讀成「圖在縮放」——與 MR-014
  「光影不連動就會讀成圖被壓歪」是同一類錯覺。讓顆粒跟著走則要每帧重新光柵化
  那張全螢幕 `overlay`，正是 MR-016 棄選 `scale` 呼吸的同一個理由
- **手機＝接受降級（用戶拍板）**：不退回網格。同一套程式碼跑到底，窄螢幕只調三個量——
  走廊半寬 300→178px、作品寬 300→232px、HUD 排列（`perspective` 一併收到 460px）。
  **幾何與相機完全不動**，一動就變成兩套要各自驗的東西。
  代價就是限制 2 說的「退化成 slideshow」，這是已知且被接受的；
  換到的是不必維護第二套版面與第二組 E2E
- **實作後才發現的三個幾何問題**（都是跑起來才顯現，不是寫的時候看得出來的）：
  1. **走得夠遠會走出房間盡頭**——房間長度原本寫死 4200px，14 件走三步後畫面右緣就露出
     牆的近端接縫。改為 `roomSpan(total)` 依件數算
  2. **牆底穿到地板以下**，在地板外側露出黑色梯形——牆與地板沒共用同一條地平線。
     現在天花板／牆／地板共用 `--ceil` / `--ground` 一組常數，改一個就要改全部
  3. **身後的作品被透視撐爆**佔掉四分之一畫面。可見窗口後方由 3 件收到 1 件，
     並讓已走過的**淡到全透明**（留 0.25 仍然顯眼，因為它被放得太大）。
     留在 DOM 是為了往回走時淡回來，不憑空跳出
- **順帶解掉第八條限制**：走廊底色改實心之後不透光，顆粒與疊印色場在裡面根本看不到，
  也就不存在「固定紋理當參考系」的錯覺。實體展場的走廊本來就是封閉的
- **錨點**：PR #21

### MR-016 · 2026-07-31 · styles
- **決策**：做 Risograph 疊印的四項視覺，並用一條界線解掉它與中性原則的衝突——
  **四項全部落在作品以外，作品圖面仍不覆蓋任何色層**：
  1. **印刷顆粒**：`body::after` 一張 feTurbulence 靜態噪點，`overlay` / opacity 0.7，
     **z-index -1**＝畫在光之上、所有內容之下，所以顆粒落在牆面不落在作品
  2. **疊印色場**：光氛層三顆 `blur(60px)` 色圓（accent × 2、counter × 1）各自 34～47s 漂移，
     `screen` 疊加，重疊處自己混出第三色
  3. **游標殘像**：`usePointerAfterimage` 每帧 lerp 出快慢兩組座標（0.22／0.10）寫成 `--trail-*`，
     停 0.7 秒後淡出。**這一項自成 `z-index: 40` 一層畫在作品之上**——等於一盞跟著游標的燈，
     與 MR-014 卡片聚光同原理（那層也是 screen 貼在作品上）
  4. **拖曳套色錯位**：長廊拖曳速度 → `misregFor()` → 軌道上的 `--misreg`，
     卡片既有的雙色疊印框位移乘上它，放手彈回
- **理由**：用戶指定要 artifact 原型裡那種疊印色圓的藝術感與探索感。那個效果的機制是
  **半透明色圓 × 疊印 × 套色錯位 × 顆粒**（artifact 的 `riso` 生成器：multiply、alpha 0.55、
  第二版偏移 1.2% 畫寬、per-pixel 噪點），不是濾鏡；照搬到暗場就是 `screen` 版。
  四項裡**顆粒的投報率最高**——零動畫、只光柵化一次，卻讓整個畫面從「CSS 漸層」變成「印刷品」
- **與 MR-008／MR-014 的衝突與收斂**：原則是「外殼中性、色彩只落在光與外框」。
  彩色色場鋪滿背景會直接推翻它。收斂點是**位置而非濃度**——全部塞進 `z-index: -1`
  的負層與光氛層，作品與其邊框永遠畫在它們之上。這條界線讓「印刷質感」與
  「作品圖面不上色」同時成立，不需要在兩者間妥協
- **棄選**：
  - **顆粒鋪在最上層**（真正的「全站」顆粒）：那會蓋在作品圖面上，直接違反 MR-008
  - **SVG `feTurbulence` 即時扭曲／WebGL 液態波動**：視覺最強但全螢幕 filter 掉帧，
    WebGL 還要引入函式庫＝重型依賴；這輪不做
  - **色場用 `scale` 呼吸**：縮放會逼 60px 模糊層每帧重新光柵化，改用 `opacity`
- **落地**：新增 `utils/motion.ts`（`lerp`／`misregFor`，純函式供單元測試）、
  `composables/usePointerAfterimage.ts`；光氛層從六層加到八層。
  單元 76→83、E2E 20→23（新增 `e2e/atmosphere.spec.js`）
- **強度是實測調出來的，不是一次到位**：第一版四項全部太保守，用戶直接反應「感受不到」。
  複驗後兩個原因——①數值太低（顆粒 0.7／色場 22～34%／殘像 40%，都要貼著螢幕才看得到）
  ②**殘像同時被兩件事吃掉**：暗角排在它之後（邊緣壓到 82% 黑），以及整層在內容之下。
  現值：顆粒 opacity 0.9 + `baseFrequency` 0.7（顆粒粗一點才在一般觀看距離讀得出來）、
  色場 46／42／32% + blur 48px、殘像 62%／52% + blur 26px
- **踩到的坑**：
  - **顆粒最初用 `soft-light`**：實測把近黑的底整片推亮，暗場的深度沒了；
    改 `overlay` 才是「暗處維持暗、有光的地方才顯顆粒」
  - **色場動畫寫在 `transform` 上會蓋掉游標視差**——改用 `translate` 獨立屬性，兩者才疊得起來
  - **殘像放在光氛層裡是死路**：`.atmo` 是 `z-index: -1` 的堆疊脈絡，子層再怎麼排都升不到
    內容之上；長廊上作品佔掉大半畫面，等於多數時候看不到。必須拉成獨立圖層
  - **AI 瀏覽器驗不了殘像**：分頁沒聚焦時 `requestAnimationFrame` 被節流，看起來像完全沒實作。
    這類東西只能走 Playwright（已記進 `docs/TESTING.md`）
  - `--misreg` 的衰減用 lerp 只會無限逼近，`toFixed(2)` 讓它卡在 1.01 不動；收尾要直接歸位
- **錨點**：PR #19

### MR-015 · 2026-07-31 · detail
- **決策**：詳情頁補上放大與平移，Phase F 收尾。三個子決定：
  1. **手勢集取「標準」**：雙擊在 fit ↔ 上限之間切換（以指標為錨）、滾輪／觸控板縮放、
     放大後拖移、邊界夾制。`←`／`→` 維持切件，切件時把縮放歸零
  2. **圖源沿用 `view` 1800px**：不新增檔案、不動 schema，放大上限＝原圖像素 1:1
     （`naturalWidth ÷ 顯示寬度`）
  3. **手機只給雙指**：`scale === 1` 時框上是 `touch-action: pan-y`，單指照常捲面板；
     放大後才切 `none` 由手勢接管單指拖移
- **理由**：Phase F 的定義是「純視圖層，不動資料」。另存原圖或加第三份 `zoom` 檔會把它
  變成資料層決策——每件多 1–5MB 進 IndexedDB 還是小事，真正的問題是**匯出 JSON 會一起膨脹**
  （`useLibrary` 匯出時把 blob 轉 data URI，base64 再 +33%），而那份 JSON 正是 MR-010
  的對外發佈路徑，大到不好進 repo 就等於把發佈路徑堵死。1:1 之上只是在放大 JPEG 的壓縮痕跡
- **棄選**：
  - **另存原圖／新增 3000px 的 `zoom` 檔**：理由同上。真要做的時機是待討論 #1
    （真作品資料到位、重估發佈路徑）那時一起評估，不是現在
  - **鍵盤 `+`／`-`／`0` 與縮放比例指示、小地圖**：`←`／`→` 已經是切件，再搶鍵位就要多一個
    「現在是縮放模式還是瀏覽模式」的狀態；60vh 的圖上小地圖收益也低
  - **手機改用全螢幕 lightbox**：能徹底避開面板捲動衝突，但多一層 UI 與返回路徑；
    `touch-action` 兩段切換已經解掉同一個問題
- **落地**：新增 `composables/useImageZoom.ts`（純函式 `maxScaleFor`／`clampScale`／
  `clampOffset`／`anchorOffset` 外露供單元測試）；`WorkDetail.vue` 的圖包進
  `.detail__viewport`（`inline-block` + `overflow: hidden`，貼著圖收縮，維持外框緊貼畫面），
  邊框與底色從圖移到框上。單元 9 tests、E2E 2 tests
- **踩到的坑**：
  - **滾輪一定要 `preventDefault`**：詳情面板自己是 `overflow-y: auto`，不擋掉就會邊縮放邊捲動
  - **拖曳不用 `setPointerCapture`**：直接沿用 MR-014 的教訓，滑鼠走 `mousedown` +
    window 監聽、觸控走 `touch*`，兩套分開，從結構上就碰不到「指標捕獲吃掉 click」
  - `clampOffset` 夾到中央時回傳 `-0`，`toBe(0)` 會假紅——測試改用 `toBeCloseTo`
- **未涵蓋**：雙指縮放沒有自動化測試（Playwright 只設 chromium 桌機、無觸控模擬），靠手動驗
- **錨點**：PR #18

### MR-014 · 2026-07-30 · wall
- **決策**：採用原型 **F 暗場光氛**為主站唯一定調，並把長廊做成有景深、可走動的展場。四件事一起做：
  1. **光氛層**（`GalleryAtmosphere.vue`）：主光暈／體積光／浮塵／低處霧氣／掃描光／暗角六層純 CSS
  2. **作品打燈**（`WorkCard.vue`）：聚光、光池、雙色錯位疊印框，新增 `card__mount` 當實體面板
  3. **長廊景深與互動**（`WorkRail.vue`）：`--focus` 驅動偏轉／退遠／變暗與光的強弱；拖曳走動、方向鍵逐件停留、走道兩端漸隱、件數標示
  4. **整體背景四選一移除**（覆蓋 MR-009 的背景層）：中性層 token 寫死在 `main.css`，`backgrounds.ts`、`BackgroundId`、`settings.background` 與編輯面板的背景切換一併刪除
- **理由**：六個原型並置比較後，用戶明確選光氛（「就是要這種設計感」）。既然只有一套呈現，
  保留四選一等於維護一條沒人走的路，還讓每個光學樣式都得寫成條件分支；
  收成單一定調後 CSS 少一層 `[data-atmosphere]` 判斷，`useAppearance` 也從兩層降為一層
- **連帶**：**書法／立體 accent 改有彩**——`書法 #3d3a35`（墨）→ 印泥朱紅 `#b7332a`、
  `立體 #6b6355`（石膏灰）→ 陶土赭 `#a9713d`；`ALL_THEME.accentDark` 改展廳青 `#4fd6e8`。
  這是 F 的前置條件：F 的機制（聚光／光池／疊印框／光暈）全部建立在有彩 accent 上，無彩會糊成灰霧。
  覆蓋 MR-008 的手工調校，但未推翻其原則——取的是材料自身的顏色，且色彩仍只落在光與外框，作品圖面不上色
- **棄選**：
  - **E／F 當日／夜雙軌**（知識檔原本的建議）：用戶要求「直接用光氛，不用切換」。
    雙軌等於維護兩套光學樣式與兩組對比度驗證，成本落在每次改樣式時
  - **保留背景切換但預設暗場**：舊 localStorage 存過 `white-cube` 的人會永遠看不到新定調
  - **在光學樣式上保留 `[data-atmosphere]` 開關**：只有一套呈現時是純粹的死條件
- **落地**：`src/prototypes/`（六方案 2482 行）與 `?lab=1` 入口整包刪除；
  新增 `usePointerParallax`（游標寫成 `--mx`／`--my`）、`counterAccent`（疊印第二色版）；
  `color.spec.ts` 補暗場配色的回歸（六類 accentDark 對比度、accent 必須有彩、色差 156 度）
- **踩到的坑**：拖曳最初用 `setPointerCapture`，指標捕獲會把後續 `click` 改派給軌道，
  **作品全部點不開**——改為 `pointerdown` 後在 window 上聽 move／up。退遠的透明度下限也調過，
  0.58 太低會讓兩側作品像蒙灰而非有距離，收在 0.74
- **錨點**：PR #17

### MR-013 · 2026-07-23 · test
- **決策**：導入兩層測試——Vitest + jsdom 測 composables／utils 邏輯，
  Playwright（僅 chromium）測瀏覽器流程與持久化。指令 `test`／`test:watch`／`coverage`／`test:e2e`。
  單元擋在 commit／deploy 前，E2E 走 PR 專屬 `ci.yml`
- **理由**：落地 `browser-automation-cost.md` 的「回歸用真測試、AI 瀏覽器只做發現」；
  canvas 圖片處理與 Blob 往 IndexedDB 的完整往返只有真實瀏覽器測得到，故必須有 E2E 這層
- **版本鎖（Node 18 限制，同 MR-003 源）**：`vitest` 釘 `~3.2`（4.x 要 Node ≥20）；
  用 jsdom 不用 happy-dom（其修 VM escape 的版本要 Node ≥20）；Playwright 設定與 E2E 測試檔
  用 `.js` 不用 `.ts`（Node 18 + ESM 下 Playwright 的 TS loader 不生效）
- **落地細節**：`fake-indexeddb` 的 structured clone 不認 jsdom Blob，故 `idb.spec.ts` 只驗
  wrapper 邏輯（Uint8Array），Blob 保真度交給 E2E。34 單元 + 14 E2E 全綠。詳見 `docs/TESTING.md`
- **未涵蓋**：`useLibrary.replaceImage`（換圖）無專測；`useAppearance`／`useMediaQuery`／`useSettings`
  由 E2E 間接覆蓋。升 Node 20 可一次解掉所有版本鎖，屬另一決策

### MR-012 · 2026-07-22 · data
- **決策**：Phase E 資料模型定案，四點——
  ①**展覽與分類正交**：分類＝媒材（無序篩選、帶主題），展覽＝策展（有序動線＋前言），作品兩者都有；
  ②**持久層收成單一 document** `artwall.library.v2`（works／categories／exhibitions／overrides／hidden），
  帶 `schemaVersion`，匯出 JSON 即該 document 本身，需寫 v1→v2 遷移；
  ③**分類個性綁在紋理上**：使用者只選名稱／顏色／紋理，`easing`／`radius`／`border` 隨紋理成組帶入，
  `accentDark` 由 accent 以 HSL 提亮自動推導並驗對比度；
  ④**展覽入口為站頭第二排**「依媒材／依展覽」模式切換，不新增頁面層級
- **理由**：分類是無序篩選、展覽是有序動線，資料結構本就不同，合併會逼出「展覽也有紋理嗎」這種答不出的問題；
  MR-010 後匯出檔＝正式發佈管道，儲存與匯出格式不一致就是多一層轉換與 bug 溫床，且無 `schemaVersion`
  會讓舊匯出檔在改版後**靜默讀壞**；動效曲線不能暴露 `cubic-bezier` 給非工程師填
- **棄選**：展覽為主軸（推翻 MR-008 已上線的分類主題，且無展覽者首頁全空）；統一成 collection（概念混淆）；
  新分類給固定中性預設（會明顯比內建六類沒個性）；維持多 key 分散（`useLibrary` 已 254 行，只會更腫）
- **影響**：`CategoryId` union → `string`，**編譯期保證消失**——`CATEGORY_MAP[work.category]` 現有 6 處
  直接索引（`WorkCard:17`／`WorkDetail:19`／`EditorPanel:322`／`works.ts:27,38`／`useAppearance:17`）必須補 fallback。
  刪分類已決定「有作品就擋下」，但**匯入 JSON 仍會帶進孤兒分類**，故防禦不可省。
  `EditorPanel`（610 行）拆為分頁式（作品／分類／展覽／站台），屬 E 的成本非額外加碼
- **落地**：拆三顆疊層 PR——①資料地基（#10：`CategoryId`→`string`、單一 document
  `artwall.library.v2`、v1→v2 遷移不刪舊 key、6 處 `categoryOf` fallback）；②分類可編輯 UI
  （#11：`EditorPanel` 610 行拆分頁 + `CategoriesTab`，accentDark 自動推導並驗暗底對比）；
  ③展覽策展層（#12：`useGallery` 展覽模式與 `?m=ex` URL、站頭「依媒材／依展覽」、`ExhibitionsTab`）。
  **按風險分階非按功能**：最易靜默讀壞資料的遷移單獨一顆，資料模型仍「一次改完」全在 #10。
  單元 34→57、E2E 14→18。無展覽時主頁面維持原樣（有 E2E 守）
- **錨點**：PR #10 → #11 → #12（依序 merge）

### MR-011 · 2026-07-22 · docs
- **決策（落地）**：本專案本機路徑扁平化為根目錄下的 `art-wall/`，`CLAUDE.md` 的 repo root 敘述同步
- ← D-004
- **落地細節**：外層包裝資料夾 `maze-resume-preview/` 移除；實體搬移須在 Claude Code session 外執行，
  因該資料夾即 session 工作目錄，搬移後需重開。外層 `.gitignore` 過渡期兩條並存，搬移後刪除舊行
- **錨點**：外層 PR #16

### MR-010 · 2026-07-22 · deploy
- **決策**：上 GitHub Pages，來源為 Actions（非 `gh-pages` 分支），push `main` 觸發；
  workflow 內含 lint 與型別檢查，沒過就不部署。repo 更名 `maze-portfolio` → `art-wall`，
  `vite base` 設 `/art-wall/`。**本機資料夾維持不動**
- **理由**：網址直接吃 repo 名，`maze-portfolio` 與已轉向的內容不符（MR-008）；
  Actions 來源不必把 `dist` 進版控，`gh-pages` 分支則會；CI 擋一次 lint／型別，
  是唯一擋得住「本機忘了跑」的地方。query string 帶狀態故不需 SPA 404 fallback
- **棄選**：`gh-pages` 分支（`dist` 進版控，與 `.gitignore` 打架）；維持舊 repo 名（網址說謊）；
  同時扁平化本機資料夾（牽動根 `prepare.md` 索引與 `.gitignore`，屬主線範圍，不夾帶）
- **影響**：解掉待討論事項 #1 的「對外生效」——路徑為匯出 JSON → 寫進 `data/works.ts` → PR → 自動部署。
  這不是後端，編輯面板「只存這台瀏覽器」的限制不變
- **錨點**：PR #5

### MR-009 · 2026-07-21 · data
- **決策**：瀏覽器內編輯只做**本機層**——圖片存 IndexedDB、文字與站台設定存 localStorage，
  透過「匯出 JSON」把資料帶進開發流程才會成為訪客看得到的版本。上傳一律壓成縮圖 800px
  與詳情用圖 1800px 兩份。另加整體背景切換（白盒／暖紙／清水模／暗展廳），與分類主題正交
- **理由**：沒有後端就沒有「對外生效」的路，硬做會給出「改完就上線」的錯覺（面板上明講此限制）。
  圖片不能放 localStorage：5MB 上限且只能存字串，一張手機照轉 base64 就吃掉大半
- **棄選**：全部塞 localStorage（配額炸）；直接接後端／GitHub API 寫回（此刻無真資料，等於先付架構成本）；
  上傳存原始檔（4000px 照片直接進牆面）
- **影響**：解決原待討論事項 #1 的「深」；新待討論事項改為「對外生效」的路徑選擇
- **→ MR-014 覆蓋其中的「整體背景切換」**：四選一整層移除，站台固定暗場光氛。
  本筆的持久層與圖片壓縮兩件決策仍然有效
- **錨點**：PR #4

### MR-008 · 2026-07-21 · wall
- **決策**：作品牆設計定案。①開場＝3.4 秒幾何動畫，Skip 常駐／只播一次／減少動態時不播，
  真互動探索留彩蛋；②分類切換取代顏色切換，換 accent＋動效曲線＋卡片邊緣＋背景紋理，
  且 accent 貫穿站頭底線、卡片外框、進度條、頁尾；③版位以 `aspect-ratio` 鎖死三種比例，
  寬螢幕水平長廊（軌道高度反推卡片寬）、窄螢幕網格
- **理由**：探索感要在開頭但不能擋路（30 秒法則）；紋理灰階、不透明度 ≤3%，且每個分類補
  `accentDark`——墨色與石膏灰在暗底會整個消失；長廊以軌道高度為唯一尺寸來源，
  交叉懸掛才不可能把版面推爆
- **棄選**：整站隨分類換皮（等於維護 4 套設計系統，且吃作品顏色）；JS masonry（跑版元凶）；
  平板兩欄（單張圖大到要捲動才看得完）
- **影響**：覆蓋 MR-005 琥珀單色；MR-001／MR-002／MR-004 的迷宮實作全數移除；移除 nes.css 依賴
- **→ MR-014 局部覆蓋**：②的 accent 手工值改有彩（書法／立體），③的長廊加上景深與拖曳；
  「中性襯托作品、彩度不落在圖面上」的原則不變
- **錨點**：PR #4

